# SITE_ADDRESS is e.g. https://203-0-113-5.sslip.io (your server IP, dashes).
# Caddy gets a real Let's Encrypt cert for an sslip.io hostname automatically.
{$SITE_ADDRESS} {
	handle /api/* {
		reverse_proxy api:3000
	}
	handle {
		root * /srv
		try_files {path} /index.html
		file_server
	}
}
